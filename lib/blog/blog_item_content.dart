import 'package:flutter/material.dart';

class BlogItemContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      color: Colors.white60,
      child: Text('This is Ambuj'),
    );
  }
}
